debug: true
spring:
  datasource:
    url: jdbc:h2:mem:taskflow
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: update
    database-platform: org.hibernate.dialect.H2Dialect
  h2:
    console:
      enabled: true

app:
  jwt:
    secret: HikkurikaettemokachiyounaiKimidakeruuruwatekiyougaiYottsutomooserowakuroshikanai
    expiration-ms: 86400000